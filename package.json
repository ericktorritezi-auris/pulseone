{
  "name": "pulseone",
  "version": "1.0.0",
  "private": true,
  "description": "PulseOne — serviço único Railway (backend NestJS + frontend Next.js no mesmo container)",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "prisma:generate": "npm run prisma:generate --prefix backend",
    "build": "npm run install:all && npm run prisma:generate && npm run build --prefix backend && npm run build --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "start": "node start.js"
  }
}
